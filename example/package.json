{
  "name": "react-timeout-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "watchify --transform [ babelify ] --entry src/example.js --outfile build/example.js -v",
    "build": "browserify --transform [ babelify ] --entry src/example.js --outfile build/example.js -v",
    "browser": "python -m SimpleHTTPServer",
    "start": "npm run build && npm run browser"
  },
  "author": "plougsgaard",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-core": "^5.8.23",
    "babel-eslint": "^4.1.1",
    "babelify": "^6.1.3",
    "browserify": "^11.0.1",
    "eslint": "^1.3.1",
    "eslint-plugin-react": "^3.3.1",
    "watchify": "^3.3.1"
  },
  "dependencies": {
    "react": "^0.13.3",
    "react-timeout": "file:.."
  }
}
